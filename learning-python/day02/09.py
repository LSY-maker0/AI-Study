"""
09 - Python 中的运算符

～ 赋值运算符：右边的值赋给左边（变量） ---> = += -= *= /= %= **=
～ 算术运算符 --> + - * / // % **
～ 关系运算符（比较运算符） ---> > < >= <= == != 产生的布尔值（True / False）
～ 逻辑运算符：多个布尔组成一个布尔 --> and（与）/ or（或）/ not (非)

Author: lsy
Date: 2025/12/3
"""
# print(True and not True)
a=int(input('a = '))
print(a>5)